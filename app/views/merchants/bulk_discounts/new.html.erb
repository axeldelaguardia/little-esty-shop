<%= link_to 'Back', :back %>


<%= form_with model: [ :merchant, @discount ], local: true do |f| %>
	<%= f.label :percentage, 'Percentage' %>
	<%= f.number_field :percentage, step: 0.1 %>
	<%= f.label :quantity_threshold, 'Quantity Threshold' %>
	<%= f.number_field :quantity_threshold %>
	<%= f.submit %>
<% end %>