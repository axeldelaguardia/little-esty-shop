<%= link_to 'Back', :back %> - <%= link_to 'Back to Merchant Dashboard', merchant_dashboard_path(@discount.merchant) %>


<h1>Bulk Discount #<%= @discount.id %></h1>

<%= render partial: 'form', locals: {
	method: :patch
}  %>